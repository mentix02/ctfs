#include <stdio.h>

int count(long number) {
  int count = 0;

  while (number != 0) {
    n /= 10;
    count++;
  }
}

unsigned long fib(unsigned long n) {

  for (long i = 0; i < n; i++) {
    
  }
  
}

int main() {
  
}
